# 🚀 StudyHub 快速开始指南

**最后更新：** 2026年2月1日

---

## 📋 目录
1. [项目状态总览](#项目状态总览)
2. [立即开始](#立即开始)
3. [关键文档导航](#关键文档导航)
4. [下一步行动](#下一步行动)

---

## 项目状态总览

### ✅ 已完成（阶段1）
- [x] 基础管理功能（机构、课程、用户、报名）
- [x] 消课功能（一键扣减课时）
- [x] 加课功能（一键增加课时）
- [x] 操作历史记录（完整追溯）
- [x] 剩余课时警告（≤5节红色提示）
- [x] 小程序端（学员管理、课时查看）

### 🚧 进行中（阶段2）
- [ ] 角色分离（教师、学员独立管理）
- [ ] 排课系统（日历视图、冲突检测）
- [ ] 课表查看（教师端、学员端）
- [ ] 教室管理

### 📅 计划中（阶段3-6）
- 考勤管理
- 通知系统
- 财务管理
- 成绩管理
- 家长端
- 数据分析

---

## 立即开始

### 第一步：阅读项目分析
📖 **必读文档：** `📊项目全面分析与发展规划.md`

**内容摘要：**
- 项目现状（技术架构、业务模型、已完成功能）
- 存在的问题（架构问题、功能缺失、用户体验）
- 世界顶级教务系统分析（Blackboard、Canvas、Schoology）
- StudyHub 发展规划（产品定位、核心功能、分阶段计划）

**关键要点：**
```
当前定位：简单的课时记录工具 ❌
建议定位：智能教务管理系统 ✅

核心问题：
1. 教师和学员混在一起（sys_users 表）
2. 缺少排课系统
3. 缺少考勤管理
4. 缺少财务管理

发展方向：
→ 对标世界一流教务系统
→ 补齐核心功能
→ 实现商业化
```

---

### 第二步：理解数据库设计
📐 **必读文档：** `📐数据库ER图设计.md`

**内容摘要：**
- 核心实体关系图（组织、教师、学员、课程、报名、排课）
- 详细表关系说明
- 数据流示例
- 索引优化建议

**关键实体：**
```
edu_organization (机构)
    ├── edu_teacher (教师)
    ├── edu_student (学员)
    ├── edu_course (课程)
    └── edu_classroom (教室)

edu_enrollment (报名记录) ← 核心业务表
    ├── student_id
    ├── course_id
    ├── total_sessions
    └── remaining_sessions

edu_schedule (排课表) ← 新增核心功能
    ├── course_id
    ├── teacher_id
    ├── classroom_id
    └── class_date + start_time + end_time

edu_class_session (课时记录) ← 操作历史
    ├── enrollment_id
    ├── action (add/subtract)
    └── num_sessions
```

---

### 第三步：查看实施方案
🎯 **必读文档：** `🎯阶段2-详细实施方案.md`

**内容摘要：**
- 数据库表结构设计（完整 SQL）
- 后端 API 开发清单
- 管理后台页面设计
- 小程序优化方案
- 测试验收标准
- 实施时间表（14天）

**实施步骤：**
```
第 1-2 天：数据库设计 ← 当前位置
第 3-5 天：后端开发
第 6-10 天：前端开发
第 11-12 天：小程序开发
第 13-14 天：测试与优化
```

---

## 关键文档导航

### 📚 规划类文档
| 文档 | 用途 | 优先级 |
|-----|------|-------|
| 📊项目全面分析与发展规划.md | 项目总览、发展方向 | ⭐⭐⭐⭐⭐ |
| 🎯阶段2-详细实施方案.md | 下一步开发计划 | ⭐⭐⭐⭐⭐ |
| 📐数据库ER图设计.md | 数据库设计 | ⭐⭐⭐⭐⭐ |

### 📝 历史文档
| 文档 | 用途 | 优先级 |
|-----|------|-------|
| ✅阶段1-完成报告.md | 阶段1功能说明 | ⭐⭐⭐ |
| 权限审查报告.md | 权限系统问题 | ⭐⭐ |
| README.md | 项目基本介绍 | ⭐⭐ |

### 🛠️ 工具类文档
| 文档 | 用途 |
|-----|------|
| 📚Docker版-完整指南.md | Docker 部署 |
| 🧪测试指南-跟着做就行.md | 测试步骤 |
| 📚MySQL终端操作-新手指南.md | 数据库操作 |

---

## 下一步行动

### 🔥 立即行动清单

#### 1. 确认需求（5分钟）
```
[ ] 已阅读 `📊项目全面分析与发展规划.md`
[ ] 已理解当前问题和发展方向
[ ] 已确认阶段2的功能优先级
```

#### 2. 数据库准备（30分钟）
```
[ ] 已阅读 `📐数据库ER图设计.md`
[ ] 已阅读 `🎯阶段2-详细实施方案.md` 第1节
[ ] 准备执行数据库创建脚本
```

#### 3. 创建数据库表（建议现在就做）
```bash
# 连接数据库
mysql -u root -p

# 选择数据库
USE gin_vue_admin;

# 执行建表语句（见下文）
```

---

## 阶段2 数据库建表语句（快速版）

### 复制以下 SQL 到你的 MySQL 客户端执行

```sql
-- ==========================================
-- StudyHub 阶段2 数据库表创建
-- ==========================================

-- 1. 教师表
CREATE TABLE `edu_teacher` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT,
  `created_at` datetime(3) DEFAULT NULL,
  `updated_at` datetime(3) DEFAULT NULL,
  `deleted_at` datetime(3) DEFAULT NULL,
  `organization_id` bigint(20) unsigned NOT NULL COMMENT '所属机构ID',
  `user_id` bigint(20) unsigned DEFAULT NULL COMMENT '关联用户ID',
  `teacher_no` varchar(50) DEFAULT NULL COMMENT '教师工号',
  `name` varchar(100) NOT NULL COMMENT '教师姓名',
  `gender` enum('male','female','other') DEFAULT 'male' COMMENT '性别',
  `phone` varchar(20) NOT NULL COMMENT '手机号',
  `email` varchar(100) DEFAULT NULL COMMENT '邮箱',
  `id_card` varchar(18) DEFAULT NULL COMMENT '身份证号',
  `avatar` varchar(255) DEFAULT NULL COMMENT '头像URL',
  `title` varchar(50) DEFAULT NULL COMMENT '职称',
  `speciality` varchar(255) DEFAULT NULL COMMENT '专业特长',
  `education` varchar(50) DEFAULT NULL COMMENT '学历',
  `graduate_school` varchar(100) DEFAULT NULL COMMENT '毕业院校',
  `teaching_age` int(11) DEFAULT 0 COMMENT '教龄',
  `status` enum('active','inactive','resigned') DEFAULT 'active' COMMENT '状态',
  `entry_date` date DEFAULT NULL COMMENT '入职日期',
  `resign_date` date DEFAULT NULL COMMENT '离职日期',
  `remark` text COMMENT '备注',
  PRIMARY KEY (`id`),
  KEY `idx_organization_id` (`organization_id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_phone` (`phone`),
  KEY `idx_deleted_at` (`deleted_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='教师信息表';

-- 2. 学员表
CREATE TABLE `edu_student` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT,
  `created_at` datetime(3) DEFAULT NULL,
  `updated_at` datetime(3) DEFAULT NULL,
  `deleted_at` datetime(3) DEFAULT NULL,
  `organization_id` bigint(20) unsigned NOT NULL COMMENT '所属机构ID',
  `user_id` bigint(20) unsigned DEFAULT NULL COMMENT '关联用户ID',
  `student_no` varchar(50) DEFAULT NULL COMMENT '学员编号',
  `name` varchar(100) NOT NULL COMMENT '学员姓名',
  `gender` enum('male','female','other') DEFAULT 'male' COMMENT '性别',
  `birthday` date DEFAULT NULL COMMENT '出生日期',
  `phone` varchar(20) NOT NULL COMMENT '联系电话',
  `avatar` varchar(255) DEFAULT NULL COMMENT '头像URL',
  `parent_name` varchar(100) DEFAULT NULL COMMENT '家长姓名',
  `parent_phone` varchar(20) DEFAULT NULL COMMENT '家长电话',
  `parent_relation` varchar(20) DEFAULT NULL COMMENT '与学员关系',
  `school` varchar(100) DEFAULT NULL COMMENT '就读学校',
  `grade` varchar(50) DEFAULT NULL COMMENT '年级',
  `address` varchar(255) DEFAULT NULL COMMENT '家庭住址',
  `status` enum('active','inactive','graduated') DEFAULT 'active' COMMENT '状态',
  `entry_date` date DEFAULT NULL COMMENT '入学日期',
  `remark` text COMMENT '备注',
  PRIMARY KEY (`id`),
  KEY `idx_organization_id` (`organization_id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_phone` (`phone`),
  KEY `idx_parent_phone` (`parent_phone`),
  KEY `idx_deleted_at` (`deleted_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='学员信息表';

-- 3. 教师-课程关联表
CREATE TABLE `edu_teacher_course` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT,
  `created_at` datetime(3) DEFAULT NULL,
  `updated_at` datetime(3) DEFAULT NULL,
  `deleted_at` datetime(3) DEFAULT NULL,
  `teacher_id` bigint(20) unsigned NOT NULL COMMENT '教师ID',
  `course_id` bigint(20) unsigned NOT NULL COMMENT '课程ID',
  `is_main_teacher` tinyint(1) DEFAULT 0 COMMENT '是否主讲教师',
  PRIMARY KEY (`id`),
  KEY `idx_teacher_id` (`teacher_id`),
  KEY `idx_course_id` (`course_id`),
  KEY `idx_deleted_at` (`deleted_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='教师-课程关联表';

-- 4. 教室表
CREATE TABLE `edu_classroom` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT,
  `created_at` datetime(3) DEFAULT NULL,
  `updated_at` datetime(3) DEFAULT NULL,
  `deleted_at` datetime(3) DEFAULT NULL,
  `organization_id` bigint(20) unsigned NOT NULL COMMENT '所属机构ID',
  `name` varchar(100) NOT NULL COMMENT '教室名称',
  `capacity` int(11) DEFAULT 0 COMMENT '容纳人数',
  `location` varchar(255) DEFAULT NULL COMMENT '位置描述',
  `facilities` text COMMENT '设施设备',
  `status` enum('available','unavailable','maintenance') DEFAULT 'available' COMMENT '状态',
  `remark` text COMMENT '备注',
  PRIMARY KEY (`id`),
  KEY `idx_organization_id` (`organization_id`),
  KEY `idx_deleted_at` (`deleted_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='教室信息表';

-- 5. 排课表
CREATE TABLE `edu_schedule` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT,
  `created_at` datetime(3) DEFAULT NULL,
  `updated_at` datetime(3) DEFAULT NULL,
  `deleted_at` datetime(3) DEFAULT NULL,
  `organization_id` bigint(20) unsigned NOT NULL COMMENT '所属机构ID',
  `course_id` bigint(20) unsigned NOT NULL COMMENT '课程ID',
  `teacher_id` bigint(20) unsigned NOT NULL COMMENT '教师ID',
  `classroom_id` bigint(20) unsigned DEFAULT NULL COMMENT '教室ID',
  `class_date` date NOT NULL COMMENT '上课日期',
  `start_time` time NOT NULL COMMENT '开始时间',
  `end_time` time NOT NULL COMMENT '结束时间',
  `duration` int(11) DEFAULT 0 COMMENT '课时时长（分钟）',
  `status` enum('scheduled','ongoing','completed','cancelled') DEFAULT 'scheduled' COMMENT '状态',
  `actual_start_time` datetime DEFAULT NULL COMMENT '实际开始时间',
  `actual_end_time` datetime DEFAULT NULL COMMENT '实际结束时间',
  `max_students` int(11) DEFAULT 0 COMMENT '最大学员数',
  `enrolled_students` int(11) DEFAULT 0 COMMENT '已报名学员数',
  `class_type` enum('regular','trial','makeup') DEFAULT 'regular' COMMENT '课程类型',
  `remark` text COMMENT '备注',
  PRIMARY KEY (`id`),
  KEY `idx_organization_id` (`organization_id`),
  KEY `idx_course_id` (`course_id`),
  KEY `idx_teacher_id` (`teacher_id`),
  KEY `idx_classroom_id` (`classroom_id`),
  KEY `idx_class_date` (`class_date`),
  KEY `idx_deleted_at` (`deleted_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='排课表';

-- 6. 排课-学员关联表
CREATE TABLE `edu_schedule_student` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT,
  `created_at` datetime(3) DEFAULT NULL,
  `updated_at` datetime(3) DEFAULT NULL,
  `deleted_at` datetime(3) DEFAULT NULL,
  `schedule_id` bigint(20) unsigned NOT NULL COMMENT '排课ID',
  `student_id` bigint(20) unsigned NOT NULL COMMENT '学员ID',
  `enrollment_id` bigint(20) unsigned NOT NULL COMMENT '报名记录ID',
  `status` enum('enrolled','attended','absent','leave') DEFAULT 'enrolled' COMMENT '状态',
  PRIMARY KEY (`id`),
  KEY `idx_schedule_id` (`schedule_id`),
  KEY `idx_student_id` (`student_id`),
  KEY `idx_enrollment_id` (`enrollment_id`),
  KEY `idx_deleted_at` (`deleted_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='排课-学员关联表';

-- 7. 修改现有表
ALTER TABLE `edu_enrollment` 
  ADD COLUMN `student_id` bigint(20) unsigned DEFAULT NULL COMMENT '学员ID' AFTER `user_id`,
  ADD KEY `idx_student_id` (`student_id`);

ALTER TABLE `sys_users` 
  ADD COLUMN `user_type` enum('admin','teacher','student','parent') DEFAULT 'admin' COMMENT '用户类型' AFTER `authority_id`,
  ADD KEY `idx_user_type` (`user_type`);

-- ==========================================
-- 完成！
-- ==========================================
SELECT '✅ 阶段2 数据库表创建成功！' AS message;
```

---

## 验证表创建

执行以下 SQL 验证表是否创建成功：

```sql
-- 查看新创建的表
SHOW TABLES LIKE 'edu_%';

-- 应该看到：
-- edu_class_session
-- edu_classroom         ← 新
-- edu_course
-- edu_enrollment
-- edu_organization
-- edu_schedule          ← 新
-- edu_schedule_student  ← 新
-- edu_student           ← 新
-- edu_teacher           ← 新
-- edu_teacher_course    ← 新
```

---

## 下一步：开始后端开发

### 准备工作
1. ✅ 数据库表已创建
2. [ ] 后端服务正常运行
3. [ ] 理解 gin-vue-admin 的代码生成器

### 开发顺序
```
第1天：教师管理 API
第2天：学员管理 API
第3天：教室管理 API
第4天：排课管理 API（核心）
第5天：冲突检测与测试
```

---

## 常见问题

### Q1: 我应该从哪里开始？
**A:** 按顺序阅读：
1. 📊项目全面分析与发展规划.md（了解全局）
2. 📐数据库ER图设计.md（理解数据结构）
3. 🎯阶段2-详细实施方案.md（开始开发）

### Q2: 数据库表很多，会不会太复杂？
**A:** 不会！这是标准的教务系统设计，参考了世界一流系统：
- Blackboard Learn
- Canvas LMS
- Schoology

我们的设计甚至比它们简化很多！

### Q3: 需要多久能完成阶段2？
**A:** 
- **全职开发**：10-14 天
- **兼职开发**：3-4 周
- **周末开发**：6-8 周

### Q4: 我可以跳过某些功能吗？
**A:** 不建议！阶段2的功能都是核心功能（P0优先级）：
- ❌ 没有教师管理 → 无法区分教师和学员
- ❌ 没有排课系统 → 无法管理上课时间
- ❌ 没有课表查看 → 用户体验极差

### Q5: 遇到问题怎么办？
**A:** 
1. 查看相关文档
2. 检查数据库表结构
3. 查看后端日志
4. 使用浏览器开发者工具

---

## 联系与反馈

如果你有任何问题或建议，请：
- 查看项目文档
- 查看代码注释
- 查看 git 提交历史

---

**准备好了吗？让我们开始打造世界级的教务系统！** 🚀

**建议操作顺序：**
1. ✅ 执行上面的数据库建表 SQL
2. ✅ 验证表创建成功
3. ✅ 告诉我："数据库已准备好！"
4. 🎯 然后我们开始后端 API 开发！
