# 🎯 StudyHub 阶段2 详细实施方案

**目标：** 角色分离 + 排课系统  
**预计工期：** 10-14 天  
**优先级：** P0（必须完成）

---

## 📋 目录
1. [数据库设计](#1-数据库设计)
2. [后端API开发](#2-后端api开发)
3. [管理后台开发](#3-管理后台开发)
4. [小程序优化](#4-小程序优化)
5. [测试验收](#5-测试验收)

---

## 1. 数据库设计

### 1.1 新增表结构

#### 教师表 (edu_teacher)
```sql
CREATE TABLE `edu_teacher` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT,
  `created_at` datetime(3) DEFAULT NULL,
  `updated_at` datetime(3) DEFAULT NULL,
  `deleted_at` datetime(3) DEFAULT NULL,
  
  -- 基本信息
  `organization_id` bigint(20) unsigned NOT NULL COMMENT '所属机构ID',
  `user_id` bigint(20) unsigned DEFAULT NULL COMMENT '关联用户ID（可登录系统）',
  `teacher_no` varchar(50) DEFAULT NULL COMMENT '教师工号',
  `name` varchar(100) NOT NULL COMMENT '教师姓名',
  `gender` enum('male','female','other') DEFAULT 'male' COMMENT '性别',
  `phone` varchar(20) NOT NULL COMMENT '手机号',
  `email` varchar(100) DEFAULT NULL COMMENT '邮箱',
  `id_card` varchar(18) DEFAULT NULL COMMENT '身份证号',
  `avatar` varchar(255) DEFAULT NULL COMMENT '头像URL',
  
  -- 职业信息
  `title` varchar(50) DEFAULT NULL COMMENT '职称',
  `speciality` varchar(255) DEFAULT NULL COMMENT '专业特长',
  `education` varchar(50) DEFAULT NULL COMMENT '学历',
  `graduate_school` varchar(100) DEFAULT NULL COMMENT '毕业院校',
  `teaching_age` int(11) DEFAULT 0 COMMENT '教龄（年）',
  
  -- 状态信息
  `status` enum('active','inactive','resigned') DEFAULT 'active' COMMENT '状态',
  `entry_date` date DEFAULT NULL COMMENT '入职日期',
  `resign_date` date DEFAULT NULL COMMENT '离职日期',
  
  -- 其他信息
  `remark` text COMMENT '备注',
  
  PRIMARY KEY (`id`),
  KEY `idx_organization_id` (`organization_id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_phone` (`phone`),
  KEY `idx_deleted_at` (`deleted_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='教师信息表';
```

#### 学员表 (edu_student)
```sql
CREATE TABLE `edu_student` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT,
  `created_at` datetime(3) DEFAULT NULL,
  `updated_at` datetime(3) DEFAULT NULL,
  `deleted_at` datetime(3) DEFAULT NULL,
  
  -- 基本信息
  `organization_id` bigint(20) unsigned NOT NULL COMMENT '所属机构ID',
  `user_id` bigint(20) unsigned DEFAULT NULL COMMENT '关联用户ID（可登录小程序）',
  `student_no` varchar(50) DEFAULT NULL COMMENT '学员编号',
  `name` varchar(100) NOT NULL COMMENT '学员姓名',
  `gender` enum('male','female','other') DEFAULT 'male' COMMENT '性别',
  `birthday` date DEFAULT NULL COMMENT '出生日期',
  `phone` varchar(20) NOT NULL COMMENT '联系电话',
  `avatar` varchar(255) DEFAULT NULL COMMENT '头像URL',
  
  -- 家长信息
  `parent_name` varchar(100) DEFAULT NULL COMMENT '家长姓名',
  `parent_phone` varchar(20) DEFAULT NULL COMMENT '家长电话',
  `parent_relation` varchar(20) DEFAULT NULL COMMENT '与学员关系',
  
  -- 其他信息
  `school` varchar(100) DEFAULT NULL COMMENT '就读学校',
  `grade` varchar(50) DEFAULT NULL COMMENT '年级',
  `address` varchar(255) DEFAULT NULL COMMENT '家庭住址',
  `status` enum('active','inactive','graduated') DEFAULT 'active' COMMENT '状态',
  `entry_date` date DEFAULT NULL COMMENT '入学日期',
  `remark` text COMMENT '备注',
  
  PRIMARY KEY (`id`),
  KEY `idx_organization_id` (`organization_id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_phone` (`phone`),
  KEY `idx_parent_phone` (`parent_phone`),
  KEY `idx_deleted_at` (`deleted_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='学员信息表';
```

#### 教师-课程关联表 (edu_teacher_course)
```sql
CREATE TABLE `edu_teacher_course` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT,
  `created_at` datetime(3) DEFAULT NULL,
  `updated_at` datetime(3) DEFAULT NULL,
  `deleted_at` datetime(3) DEFAULT NULL,
  
  `teacher_id` bigint(20) unsigned NOT NULL COMMENT '教师ID',
  `course_id` bigint(20) unsigned NOT NULL COMMENT '课程ID',
  `is_main_teacher` tinyint(1) DEFAULT 0 COMMENT '是否主讲教师',
  
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_teacher_course` (`teacher_id`, `course_id`, `deleted_at`),
  KEY `idx_teacher_id` (`teacher_id`),
  KEY `idx_course_id` (`course_id`),
  KEY `idx_deleted_at` (`deleted_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='教师-课程关联表';
```

#### 教室表 (edu_classroom)
```sql
CREATE TABLE `edu_classroom` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT,
  `created_at` datetime(3) DEFAULT NULL,
  `updated_at` datetime(3) DEFAULT NULL,
  `deleted_at` datetime(3) DEFAULT NULL,
  
  `organization_id` bigint(20) unsigned NOT NULL COMMENT '所属机构ID',
  `name` varchar(100) NOT NULL COMMENT '教室名称',
  `capacity` int(11) DEFAULT 0 COMMENT '容纳人数',
  `location` varchar(255) DEFAULT NULL COMMENT '位置描述',
  `facilities` text COMMENT '设施设备（JSON）',
  `status` enum('available','unavailable','maintenance') DEFAULT 'available' COMMENT '状态',
  `remark` text COMMENT '备注',
  
  PRIMARY KEY (`id`),
  KEY `idx_organization_id` (`organization_id`),
  KEY `idx_deleted_at` (`deleted_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='教室信息表';
```

#### 排课表 (edu_schedule)
```sql
CREATE TABLE `edu_schedule` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT,
  `created_at` datetime(3) DEFAULT NULL,
  `updated_at` datetime(3) DEFAULT NULL,
  `deleted_at` datetime(3) DEFAULT NULL,
  
  -- 基本信息
  `organization_id` bigint(20) unsigned NOT NULL COMMENT '所属机构ID',
  `course_id` bigint(20) unsigned NOT NULL COMMENT '课程ID',
  `teacher_id` bigint(20) unsigned NOT NULL COMMENT '教师ID',
  `classroom_id` bigint(20) unsigned DEFAULT NULL COMMENT '教室ID',
  
  -- 时间信息
  `class_date` date NOT NULL COMMENT '上课日期',
  `start_time` time NOT NULL COMMENT '开始时间',
  `end_time` time NOT NULL COMMENT '结束时间',
  `duration` int(11) DEFAULT 0 COMMENT '课时时长（分钟）',
  
  -- 状态信息
  `status` enum('scheduled','ongoing','completed','cancelled') DEFAULT 'scheduled' COMMENT '状态',
  `actual_start_time` datetime DEFAULT NULL COMMENT '实际开始时间',
  `actual_end_time` datetime DEFAULT NULL COMMENT '实际结束时间',
  
  -- 其他信息
  `max_students` int(11) DEFAULT 0 COMMENT '最大学员数',
  `enrolled_students` int(11) DEFAULT 0 COMMENT '已报名学员数',
  `class_type` enum('regular','trial','makeup') DEFAULT 'regular' COMMENT '课程类型',
  `remark` text COMMENT '备注',
  
  PRIMARY KEY (`id`),
  KEY `idx_organization_id` (`organization_id`),
  KEY `idx_course_id` (`course_id`),
  KEY `idx_teacher_id` (`teacher_id`),
  KEY `idx_classroom_id` (`classroom_id`),
  KEY `idx_class_date` (`class_date`),
  KEY `idx_deleted_at` (`deleted_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='排课表';
```

#### 排课-学员关联表 (edu_schedule_student)
```sql
CREATE TABLE `edu_schedule_student` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT,
  `created_at` datetime(3) DEFAULT NULL,
  `updated_at` datetime(3) DEFAULT NULL,
  `deleted_at` datetime(3) DEFAULT NULL,
  
  `schedule_id` bigint(20) unsigned NOT NULL COMMENT '排课ID',
  `student_id` bigint(20) unsigned NOT NULL COMMENT '学员ID',
  `enrollment_id` bigint(20) unsigned NOT NULL COMMENT '报名记录ID',
  `status` enum('enrolled','attended','absent','leave') DEFAULT 'enrolled' COMMENT '状态',
  
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_schedule_student` (`schedule_id`, `student_id`, `deleted_at`),
  KEY `idx_schedule_id` (`schedule_id`),
  KEY `idx_student_id` (`student_id`),
  KEY `idx_enrollment_id` (`enrollment_id`),
  KEY `idx_deleted_at` (`deleted_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='排课-学员关联表';
```

### 1.2 修改现有表

#### 修改 edu_enrollment 表
```sql
-- 添加 student_id 字段
ALTER TABLE `edu_enrollment` 
  ADD COLUMN `student_id` bigint(20) unsigned DEFAULT NULL COMMENT '学员ID' AFTER `user_id`,
  ADD KEY `idx_student_id` (`student_id`);

-- 说明：保留 user_id 用于向后兼容，逐步迁移到 student_id
```

#### 修改 sys_users 表
```sql
-- 添加用户类型字段
ALTER TABLE `sys_users` 
  ADD COLUMN `user_type` enum('admin','teacher','student','parent') DEFAULT 'admin' COMMENT '用户类型' AFTER `authority_id`,
  ADD KEY `idx_user_type` (`user_type`);
```

### 1.3 数据迁移脚本

```sql
-- 数据迁移脚本（待实施时调整）
-- 1. 将现有 sys_users 中角色为教师的用户迁移到 edu_teacher
-- 2. 将现有 sys_users 中角色为学员的用户迁移到 edu_student
-- 3. 更新 edu_enrollment 表的 student_id 字段

-- TODO: 具体迁移逻辑根据实际数据情况编写
```

---

## 2. 后端API开发

### 2.1 教师管理 API

#### 文件结构
```
server/
├── model/
│   └── edu_organization/
│       ├── edu_teacher.go          # 教师模型
│       └── request/
│           └── edu_teacher.go       # 请求参数
├── service/
│   └── edu_organization/
│       └── edu_teacher.go           # 业务逻辑
├── api/v1/
│   └── edu_organization/
│       └── edu_teacher.go           # API 控制器
└── router/
    └── edu_organization/
        └── edu_teacher.go           # 路由注册
```

#### API 列表
```go
// 教师管理 API
POST   /eduTeacher/createEduTeacher          // 创建教师
DELETE /eduTeacher/deleteEduTeacher          // 删除教师
DELETE /eduTeacher/deleteEduTeacherByIds     // 批量删除
PUT    /eduTeacher/updateEduTeacher          // 更新教师
GET    /eduTeacher/findEduTeacher            // 查询单个
GET    /eduTeacher/getEduTeacherList         // 获取列表（分页）
GET    /eduTeacher/getEduTeachersByCourse    // 根据课程获取教师列表
POST   /eduTeacher/bindCourse                // 绑定课程
DELETE /eduTeacher/unbindCourse              // 解绑课程
```

### 2.2 学员管理 API

#### API 列表
```go
// 学员管理 API
POST   /eduStudent/createEduStudent          // 创建学员
DELETE /eduStudent/deleteEduStudent          // 删除学员
DELETE /eduStudent/deleteEduStudentByIds     // 批量删除
PUT    /eduStudent/updateEduStudent          // 更新学员
GET    /eduStudent/findEduStudent            // 查询单个
GET    /eduStudent/getEduStudentList         // 获取列表（分页）
GET    /eduStudent/getEduStudentsByCourse    // 根据课程获取学员列表
```

### 2.3 教室管理 API

```go
// 教室管理 API
POST   /eduClassroom/createEduClassroom      // 创建教室
DELETE /eduClassroom/deleteEduClassroom      // 删除教室
PUT    /eduClassroom/updateEduClassroom      // 更新教室
GET    /eduClassroom/findEduClassroom        // 查询单个
GET    /eduClassroom/getEduClassroomList     // 获取列表
GET    /eduClassroom/checkAvailability       // 检查教室可用性
```

### 2.4 排课管理 API

```go
// 排课管理 API
POST   /eduSchedule/createEduSchedule        // 创建排课
DELETE /eduSchedule/deleteEduSchedule        // 删除排课
PUT    /eduSchedule/updateEduSchedule        // 更新排课
GET    /eduSchedule/findEduSchedule          // 查询单个
GET    /eduSchedule/getEduScheduleList       // 获取列表（支持多条件筛选）
POST   /eduSchedule/checkConflict            // 检查排课冲突
POST   /eduSchedule/addStudent               // 添加学员到排课
DELETE /eduSchedule/removeStudent            // 从排课移除学员
GET    /eduSchedule/getScheduleByTeacher     // 获取教师课表
GET    /eduSchedule/getScheduleByStudent     // 获取学员课表
GET    /eduSchedule/getScheduleByClassroom   // 获取教室课表
POST   /eduSchedule/batchCreate              // 批量创建排课（周期性）
```

### 2.5 冲突检测算法

```go
// 排课冲突检测逻辑
type ConflictCheckRequest struct {
    TeacherID   uint      `json:"teacherId"`
    ClassroomID uint      `json:"classroomId"`
    ClassDate   string    `json:"classDate"`
    StartTime   string    `json:"startTime"`
    EndTime     string    `json:"endTime"`
    ExcludeID   uint      `json:"excludeId"` // 编辑时排除自己
}

// 检查项：
// 1. 教师时间冲突（同一时间段不能有两节课）
// 2. 教室占用冲突（同一时间段不能被占用）
// 3. 学员时间冲突（学员同一时间不能上两节课）
```

---

## 3. 管理后台开发

### 3.1 教师管理页面

#### 页面路径
```
web/src/view/eduTeacher/
├── eduTeacher.vue          # 列表页
├── eduTeacherForm.vue      # 表单页（创建/编辑）
└── eduTeacherCourse.vue    # 课程绑定页
```

#### 列表页功能
```vue
<template>
  <!-- 功能区 -->
  - 搜索框：姓名、手机号、工号
  - 筛选：状态（在职/离职）、职称
  - 按钮：新增教师、批量删除、导出

  <!-- 表格列 -->
  - 教师工号
  - 姓名
  - 性别
  - 手机号
  - 职称
  - 教龄
  - 状态（彩色标签）
  - 操作：编辑、删除、绑定课程、查看课表
</template>
```

#### 表单页功能
```vue
<template>
  <!-- 基本信息 -->
  - 姓名（必填）
  - 性别
  - 手机号（必填）
  - 邮箱
  - 身份证号
  - 头像上传

  <!-- 职业信息 -->
  - 教师工号（自动生成）
  - 职称
  - 专业特长
  - 学历
  - 毕业院校
  - 教龄

  <!-- 状态信息 -->
  - 状态
  - 入职日期
  - 备注
</template>
```

### 3.2 学员管理页面

#### 页面路径
```
web/src/view/eduStudent/
├── eduStudent.vue          # 列表页
├── eduStudentForm.vue      # 表单页（创建/编辑）
└── eduStudentSchedule.vue  # 课表页
```

#### 列表页功能
```vue
<template>
  <!-- 功能区 -->
  - 搜索框：姓名、手机号、学员编号
  - 筛选：状态、年级
  - 按钮：新增学员、批量导入、导出

  <!-- 表格列 -->
  - 学员编号
  - 姓名
  - 性别
  - 年龄
  - 手机号
  - 家长姓名
  - 家长电话
  - 报名课程数
  - 剩余课时
  - 状态
  - 操作：编辑、删除、查看课表、查看报名记录
</template>
```

### 3.3 排课管理页面

#### 页面路径
```
web/src/view/eduSchedule/
├── eduSchedule.vue         # 日历视图（主页）
├── eduScheduleForm.vue     # 排课表单
├── eduScheduleList.vue     # 列表视图
└── components/
    ├── WeekCalendar.vue    # 周视图组件
    ├── MonthCalendar.vue   # 月视图组件
    └── ConflictDialog.vue  # 冲突提示组件
```

#### 日历视图（核心功能）
```vue
<template>
  <!-- 视图切换 -->
  - 周视图 / 月视图

  <!-- 筛选条件 -->
  - 教师筛选
  - 课程筛选
  - 教室筛选

  <!-- 日历展示 -->
  - 显示每天的排课
  - 点击单元格：快速创建排课
  - 点击已排课：查看详情/编辑
  - 拖拽排课：调整时间（可选）
  - 颜色区分：不同课程不同颜色

  <!-- 冲突提示 -->
  - 创建时实时检测冲突
  - 红色标记冲突时段
</template>
```

#### 排课表单
```vue
<template>
  <!-- 基本信息 -->
  - 课程（下拉选择）
  - 教师（下拉选择，显示该课程的授课教师）
  - 教室（下拉选择）

  <!-- 时间信息 -->
  - 上课日期（日期选择器）
  - 开始时间（时间选择器）
  - 结束时间（时间选择器）
  - 课时时长（自动计算）

  <!-- 学员信息 -->
  - 学员列表（多选，显示已报名该课程的学员）
  - 最大学员数

  <!-- 其他 -->
  - 课程类型（常规课/试听课/补课）
  - 备注

  <!-- 冲突检测 -->
  - 实时显示冲突提示
  - 教师冲突
  - 教室冲突
  - 学员冲突
</template>
```

### 3.4 课表查看页面

#### 教师课表
```vue
<template>
  <!-- 展示教师的周课表 -->
  - 横轴：星期一到星期日
  - 纵轴：时间段（8:00-22:00）
  - 单元格内容：
    - 课程名称
    - 教室
    - 学员数
    - 时间段
</template>
```

#### 学员课表
```vue
<template>
  <!-- 展示学员的周课表 -->
  - 横轴：星期一到星期日
  - 纵轴：时间段
  - 单元格内容：
    - 课程名称
    - 教师姓名
    - 教室
    - 时间段
  - 下一节课提醒
</template>
```

---

## 4. 小程序优化

### 4.1 学员端课表

#### 页面路径
```
wx/src/pages/schedule/
└── index.vue               # 我的课表
```

#### 功能设计
```vue
<template>
  <!-- 顶部导航 -->
  - 左右切换周次

  <!-- 周课表 -->
  - 横向滑动
  - 今天高亮
  - 课程卡片：
    - 课程名称
    - 时间段
    - 教师姓名
    - 教室
    - 状态（未开始/进行中/已结束）

  <!-- 下一节课提醒 -->
  - 卡片展示
  - 倒计时
  - 一键导航（地图）
</template>
```

### 4.2 教师端功能（可选）

#### 教师小程序页面
```
wx/src/pages/teacher/
├── schedule.vue            # 我的课表
└── students.vue            # 学员名单
```

---

## 5. 测试验收

### 5.1 单元测试

```
✅ 教师管理
  - 创建教师
  - 更新教师
  - 删除教师
  - 查询教师
  - 绑定课程

✅ 学员管理
  - 创建学员
  - 更新学员
  - 删除学员
  - 查询学员

✅ 排课管理
  - 创建排课
  - 冲突检测（教师/教室/学员）
  - 批量创建排课
  - 查询课表
```

### 5.2 集成测试

```
✅ 完整业务流程
  1. 创建教师 A
  2. 创建学员 B
  3. 教师 A 绑定课程 C
  4. 学员 B 报名课程 C
  5. 为课程 C 排课
  6. 添加学员 B 到排课
  7. 教师 A 查看课表
  8. 学员 B 查看课表
```

### 5.3 验收清单

```
[ ] 可以独立管理教师（增删改查）
[ ] 可以独立管理学员（增删改查）
[ ] 可以管理教室（增删改查）
[ ] 教师可以绑定多门课程
[ ] 可以创建排课
[ ] 排课时自动检测冲突
[ ] 可以查看周课表（教师视角）
[ ] 可以查看周课表（学员视角）
[ ] 小程序端可以查看课表
[ ] 数据按机构隔离（权限控制）
```

---

## 6. 实施时间表

### 第 1-2 天：数据库设计
- [x] 设计表结构
- [ ] Review 并确认
- [ ] 创建数据库表
- [ ] 编写数据迁移脚本

### 第 3-5 天：后端开发
- [ ] 教师管理 API
- [ ] 学员管理 API
- [ ] 教室管理 API
- [ ] 排课管理 API
- [ ] 冲突检测算法

### 第 6-10 天：前端开发
- [ ] 教师管理页面
- [ ] 学员管理页面
- [ ] 教室管理页面
- [ ] 排课日历页面
- [ ] 课表查看页面

### 第 11-12 天：小程序开发
- [ ] 学员端课表页面
- [ ] 下一节课提醒功能

### 第 13-14 天：测试与优化
- [ ] 单元测试
- [ ] 集成测试
- [ ] 性能优化
- [ ] Bug 修复

---

## 7. 技术难点与解决方案

### 7.1 冲突检测算法

**难点：** 如何高效检测教师、教室、学员的时间冲突？

**解决方案：**
```go
// 时间区间重叠判断
func isTimeOverlap(start1, end1, start2, end2 time.Time) bool {
    return start1.Before(end2) && end1.After(start2)
}

// 查询数据库中的冲突排课
SELECT * FROM edu_schedule 
WHERE class_date = ? 
  AND teacher_id = ? 
  AND id != ? 
  AND deleted_at IS NULL
  AND (
    (start_time < ? AND end_time > ?) OR
    (start_time < ? AND end_time > ?) OR
    (start_time >= ? AND end_time <= ?)
  )
```

### 7.2 日历组件性能优化

**难点：** 大量排课数据渲染性能问题

**解决方案：**
- 虚拟滚动（只渲染可见区域）
- 按需加载（只加载当前周/月数据）
- 防抖和节流（滚动、拖拽事件）

### 7.3 数据迁移

**难点：** 如何平滑迁移现有数据？

**解决方案：**
- 保留 `user_id` 字段向后兼容
- 逐步迁移数据到 `student_id`
- 提供数据校验工具
- 双写一段时间后再完全切换

---

## 8. 注意事项

### 8.1 权限控制
- 所有 API 都要加 `organization_id` 过滤
- 教师只能看到自己的课表
- 学员只能看到自己的课表

### 8.2 数据验证
- 手机号格式验证
- 身份证号格式验证
- 时间范围合理性检查
- 教室容量限制

### 8.3 用户体验
- 操作反馈及时
- 错误提示友好
- 加载状态明确
- 移动端适配

---

## 9. 下一步

完成阶段 2 后，我们将进入：
- **阶段 3**：考勤管理与通知系统
- **阶段 4**：财务管理
- **阶段 5**：成绩管理与家长端

---

**准备好开始了吗？让我们从数据库设计开始！** 🚀
